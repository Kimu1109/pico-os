# pico-os チャット プロトコル v1

pico-os のチャットアプリ(`ChatScene`)と Web クライアントが、自前のチャットサーバと話すための取り決め。
参照実装は **`server/chat/chat_server.py`**(標準ライブラリのみ)。本書と実装が食い違う場合は、
**実装のほうが検証済み**(`script/host_test/run_net.sh` の `chat_net_test`)なので実装を信じて本書を直すこと。

考え方は `PROTOCOL.md`(ドキュメントサーバ)と同じで、**pico-os が苦手なことは全部サーバ側でやる**:
JSON を使わず1行1件の TSV、応答は常に長さ付き、上限はサーバが守る。

## クライアント側の制約(仕様の根拠)

| 項目 | 値 | 影響 |
|---|---|---|
| JSONパーサ | 無い | 応答は行指向TSV |
| 同時に張れる接続 | 1本 | ロングポーリングすると待っている間送信できない → pico-os は数秒ごとの問い合わせ |
| TLSのハンドシェイク | 実機で1〜2秒、その間画面が止まる | **サーバは keep-alive に対応すること**(pico-os は接続を使い回す) |
| 発言1件の本文 | 500バイトまで持てる | サーバが上限を守る |
| 名前 | 45バイトまで持てる | 同上 |
| 時計 | NTP同期前は不正 | 時刻はサーバが付ける |

## トランスポート

- **必須**: HTTP/1.1。公開するなら HTTPS(TLS 1.2 を受け付けること。pico-os の BearSSL は 1.3 を話さない)
- **必須**: 応答には必ず `Content-Length` を付ける(chunked も可だが、参照実装は使わない)。**圧縮しない**
- **必須**: `Connection: keep-alive` の要求には、応答後も接続を開けておく。無通信の接続は閉じてよい
  (参照実装は120秒。pico-os は切られていたら繋ぎ直す)
- 文字コードは UTF-8 固定

## 認証

どの API も、次のどちらかが要る(無い/違うときは `401`)。

| 使う側 | 方法 |
|---|---|
| pico-os | `Authorization: Bearer <トークン>`。トークンは base64url の文字だけ(参照実装は43文字) |
| Web | `POST /api/v1/login` で受け取る Cookie `pc_session`。**書き込み(POST)には `X-Pico-Chat: 1` ヘッダも必須**(他のサイトから送らせないため) |

## 共通の形式

- 成功した応答の本文は `text/plain; charset=utf-8` の **1行1件のTSV**。行の区切りは LF
- 各欄に **タブ・改行を含めない**(名前はサーバが空白へ潰す)
- **発言の本文だけはエスケープする**: `\` → `\\`、改行 → `\n`、タブ → `\t`。
  クライアントは知らない `\x` を `x` として読む(前方互換)
- **行の後ろに欄が増えても、クライアントは読まずに無視する**(前方互換)
- 失敗した応答(4xx/5xx)の本文の **1行目は利用者へそのまま見せられる理由**(日本語)

## API

### `GET /api/v1/rooms` — 部屋の一覧

```
<部屋id>\t<部屋名>\t<最新の発言id>\t<自分の未読の数>
```

発言が無い部屋の最新idは `0`。並びは部屋id順。

### `GET /api/v1/rooms/<部屋id>/messages` — 発言

| クエリ | 意味 |
|---|---|
| (無し) | 最新の `limit` 件 |
| `after=<id>` | その id より新しい発言を古い順に最大 `limit` 件 |
| `before=<id>` | その id より古い発言のうち新しい `limit` 件(遡って読む) |
| `limit=<n>` | 1〜50(既定20) |
| `wait=<秒>` | `after` と一緒に使う。新しい発言が無ければ最大その秒数(30まで)応答を待つ(ロングポーリング。Web用) |

```
<発言id>\t<UNIX時刻(秒)>\t<発言者の表示名>\t<本文(エスケープ済み)>
```

- 常に**古い順**に並ぶ。発言idは増え続ける整数なので、クライアントは最後に受け取ったidだけ覚えておけばよい
- **`before` 以外で取った分は既読になる**(最後の発言idまで)
- 部屋が無ければ `404`

### `POST /api/v1/rooms/<部屋id>/messages` — 発言する

本文 = 発言(UTF-8 のプレーンテキスト、`Content-Length` 必須)。

- 成功は `201` で `<発言id>\t<UNIX時刻>`。自分の発言までは既読になる
- 空は `400`、500バイトを超えると `413`
- CRLF/CR は LF へ、タブは空白4つへ、それ以外の制御文字は落とす

### `POST /api/v1/rooms` — 部屋を作る

本文 = 部屋名(45バイトまで)。成功は `201` で `<部屋id>\t<部屋名>`、同名があれば `409`。

### Web 用

| API | 内容 |
|---|---|
| `POST /api/v1/login` | `login=...&password=...`(フォーム形式)。成功で Cookie を渡し `<ログイン名>\t<表示名>` |
| `POST /api/v1/logout` | Cookie を消す |
| `GET /api/v1/me` | `<ログイン名>\t<表示名>` |
| `POST /api/v1/token` | pico-os 用のトークンを発行し直して本文で返す(前のトークンは無効になる。サーバはハッシュしか持たないので、平文はこの応答でしか見られない) |
| `GET /` | Web クライアント |
| `GET /.well-known/pico-chat` | `version\t1` / `max_text_bytes\t500`(サーバの確認用) |

## 制限値の一覧

| 項目 | 値 |
|---|---|
| 発言1件の本文 | 500バイト |
| 表示名・部屋名 | 45バイト |
| ログイン名 | 英数字と `_` `-`、32文字 |
| `limit` | 1〜50 |
| `wait` | 0〜30秒 |
| リクエスト本文 | 4096バイト |
