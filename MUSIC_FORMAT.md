# pico-os 曲データ形式(pico-os MML)仕様

pico-os のチップチューン音源(`src/sound/Chip_Synth`、4チャンネル)で鳴らす曲の書き方。
**MML(Music Macro Language)** を pico-os 向けに絞った方言で、1曲を1つのテキストファイル(`.mml`)に書く。

> **現状**: **実装済み**(読み取り `src/sound/Mml_Compiler`、2コア目のシーケンサー `src/sound/Music_Player`、
> 窓口 `SoundFunctions::MusicPlay*`、Luaの `pico.music_*`、ミュージックアプリ `MusicScene`)。
> 挙動は `script/host_test/music_test.cpp` で固定してある。本書と実装が食い違えば、実装を信じて本書を直す。
> サンプル: `pc/sdcard/music/demo.mml`(短いループ)、`pc/sdcard/music/sample.mml`(ほとんどの命令を使った見本)

## なぜMMLか(他の形式との関係)

| 形式 | 扱い |
|---|---|
| **MML(本書)** | **標準**。音源の要素(4チャンネル・波形・音量・減衰・長さ)がそのまま書け、1曲数KBのテキストで人が読み書きできる |
| MIDI(SMF) | 取り込み用。まずはPC側の変換スクリプト(`script/midi2mml.py`、未)でMMLへ直す。4音への間引きと音色の割り当ては機械任せだと崩れるので、変換後に人が直せる形を経由する |
| VGM(ゲームボーイ)/ GBS | ゲームボーイの音源チップを再現した後(SUMMARY.md #11「GB対応」)に検討 |
| MOD/XM、WAV/MP3 | 対象外(サンプルを鳴らす形式で、チップチューンの合成ではない) |

どの形式も本体の中では**同じ「演奏データ」へ変換してから鳴らす**(下の「本体の中での扱い」)。形式を増やすときは変換を1つ足すだけで、
2コア目の演奏処理は変えない。

## 全体像

```
/music/xxx.mml ──(1コア目: 読み取り・変換)──→ 演奏データ ──→ 2コア目: シーケンサー → 音源 → I2S
```

- 読み取りと変換は**1コア目**で、曲を開いたときに1回だけ行う(誤りはここで行・列つきで分かる)。
- 鳴らすのは**2コア目**。時間はサンプル数で数えるので、1コア目が描画や通信で止まってもテンポは揺れない。

## クライアント側の制約(仕様の根拠)

| 項目 | 値 | 理由 |
|---|---|---|
| チャンネル数 | 4(`A` `B` `C` `D`) | 音源のチャンネル数 |
| 同時発音 | 1チャンネル1音 | 和音は複数チャンネルへ分けて書く |
| 1行の長さ | 512バイトまで | 読み取りは1行ずつ(ファイル全体をRAMへ載せない) |
| 演奏データ | 6KiBまで | 読み取った結果の置き場。4小節×4チャンネルで約350バイトなので、数分の曲が入る |
| 時間の細かさ | 4分音符 = 48ティック | 64分音符(3)・3連8分(16)・3連16分(8)まで割り切れる |
| テンポ | 30〜300(4分音符/分) | |
| 繰り返しの入れ子 | 4段まで | 演奏データの中の固定長のスタック |
| 文字コード | UTF-8 | 曲名等に日本語を書ける。本文(音符)はASCIIだけ |

## ファイルの形

```
; 行頭が ; の行と、行の途中の ; から後ろは注釈
#title    デモ曲
#composer 作者名
#tempo    200

#macro k = @noise o6 v13 E-1 c16 r16      ; ドラムの定義(下の「マクロ」)
#macro h = @noise_short o8 v4 c32 r16.
#macro s = @noise o8 v10 E-1 c8

A @pulse25 v12 E-6 o5 l4   e g a g8 e8 | d e c2 | e g a > c < | b g a2
B @pulse12 v6  E-2 o4 l8   c e g e < a > c e c | < f a > c < a g b > d < b >
B                          c e g e < a > c e c | < f a g b > c e g e
C @triangle v15    o3 l4   c c < a a > | < f f g g > | c c < a a > | < f g > c2
D                          [ $k $h $s $h ]8          ; 8分×4 = 1小節を8回
```

- **1行 = 1つの命令行**。行頭が `#` なら**ヘッダ**、`A`〜`D`(複数並べてもよい)なら**トラック行**、それ以外は誤り。
- **同じチャンネルの行はつながる**(上の`B`は2行で1本)。
- `AB` のように並べると、同じ内容を複数のチャンネルへ書いたことになる(ユニゾン等)。
- `|` は小節線で、**読み飛ばす**(見やすくするためだけのもの)。空白・タブも読み飛ばす。

### ヘッダ

| ヘッダ | 意味 | 既定 |
|---|---|---|
| `#title 文字列` | 曲名(一覧に出す) | ファイル名 |
| `#composer 文字列` | 作者 | 空 |
| `#tempo n` | 最初のテンポ | 120 |
| `#macro 名前 = 内容` | マクロの定義(下) | |
| (知らないヘッダ) | **読み飛ばす**(将来ヘッダを足しても古いpico-osが壊れないように) | |

## トラックの中の命令

大文字小文字は区別する(音符は小文字。大文字の命令は `E` と `L` だけ)。数値は10進数。

### 音符と休符

| 書き方 | 意味 |
|---|---|
| `c d e f g a b` | 音符。後ろに `+` か `#`(半音上げ)、`-`(半音下げ)を付けられる |
| `c4` `c8.` `c16..` | 長さ(1 2 4 8 16 32 64 と、3連の 3 6 12 24 48 96 192。全音符=192ティックを割り切れる数)。`.` は付点(何個でも。割り切れなくなったら誤り) |
| `c` | 長さを省くと `l` の長さ |
| `c4^8` | タイ(`^` の後ろの長さを足す。`^^` も可) |
| `r` `r4` | 休符 |
| `n60` `n60,8` | MIDIノート番号で直接(60 = オクターブ4のド)。`,`の後ろが長さ。`k` の移調は掛かる |

### 状態を変える命令(次の音符から効く)

| 書き方 | 範囲 | 既定 | 意味 |
|---|---|---|---|
| `o n` | 0〜8 | 4 | オクターブ。`o4 a` = 440Hz |
| `>` / `<` | | | オクターブを1つ上げる / 下げる |
| `l n` | 長さ | 4 | 長さを省いた音符の長さ |
| `t n` | 30〜300 | `#tempo` | テンポ(**全チャンネル共通**。書いたトラックの、その時点から) |
| `v n` | 0〜15 | 15 | 音量(鳴り始め) |
| `@名前` / `@n` | | `@pulse50` | 波形。名前は `pulse12` `pulse25` `pulse50` `pulse75` `triangle` `saw` `noise` `noise_short`(Luaの`pico.sound_play`と同じ)、数字はこの順に0〜7 |
| `E n` | -7〜7 | 0 | 減衰/増加(0=一定、負=|n|/64秒ごとに1段下げる、正=上げる)。音符ごとに最初から掛かり直す。**大文字**(小文字の `e` は音符のミ。`e-6` は「ミ♭の6分音符」と区別できないため) |
| `q n` | 1〜8 | 7 | 音を切る早さ。音符の長さの n/8 だけ鳴らし、残りは無音(8で切れ目なし) |
| `k n` | -24〜24 | 0 | 移調(半音単位)。以降の音符の高さをずらす |

**ノイズ(`@noise` / `@noise_short`)の高さ**: 音符の高さの周波数×16 を「ザー」の粗さとして使う
(`o6 c` ≒ 16.7kHz。高いほど細かい)。Luaの「チップチューン」アプリの鍵盤と同じ対応。

### 繰り返しとループ

| 書き方 | 意味 |
|---|---|
| `[ ... ]n` | 中身を n 回(2〜255、省略で2)。入れ子は4段まで |
| `[ ... : ... ]n` | 最後の回だけ `:` の後ろを飛ばして抜ける(1番/2番かっこ) |
| `L` | **このトラックのループ位置**。最後まで行ったらここへ戻る(1トラックに1つ) |

- 曲の終わりは「`L`の無いトラックが全部終わったとき」。全トラックに`L`があれば止めるまで鳴り続ける。
- 長さの違うトラックに`L`を付けると、ループの周期がずれていく(書く側の責任。警告は出す)。

### マクロ

`#macro 名前 = 内容` で定義し、トラックの中で `$名前` と書くとその場に内容が入る。名前は英数字と`_`で16文字まで。
**マクロの中で変えた状態(波形・音量・オクターブ等)はマクロを抜けると元に戻る**(ドラムの定義をどこに差し込んでも、
周りの音符に影響しないように)。マクロの中から別のマクロは呼べない(1段だけ)。

## 誤りの扱い

- **知らない命令・範囲外の値は誤り**として、行と列と理由を返して読み込みを失敗させる(黙って飛ばすと、書いた人が気づけない)。
- 知らない**ヘッダ**だけは読み飛ばす(前方互換)。
- 誤りはLuaの `pico.music_play` 等が `nil, "3行12列: v の後ろは0〜15です"` のように返す。マクロの中の誤りは、そのマクロを呼んだ `$` の位置で `マクロ$k: …` と報告する。
- 読み取れても気になる点は**警告**としてログへ出す(`L` から後ろの長さがチャンネルで違う、`[ ]` の中でオクターブ/移調が元に戻っていない)。
- 読めなかったときは、鳴っている曲はそのまま鳴り続ける。

## 効果音との同居

曲を鳴らしている間に `pico.sound_play()` 等で効果音を鳴らすと、**効果音がそのチャンネルを借りる**。

- 借りている間、曲のそのチャンネルは**黙るが進行は止まらない**(他のチャンネルとずれない)。
- 効果音が終わったら、曲は**次の音符から**そのチャンネルへ戻る(鳴りかけの音を途中から鳴らし直しはしない)。
- 曲を止めても効果音は止まらない(逆も同じ)。

## 本体の中での扱い(実装。仕様ではない)

- 読み取り(`MmlCompiler`)は1コア目。1周目でヘッダとマクロを集め、**チャンネルごとに頭から読み直して**命令列を書く
  (SDのファイルなら全体をRAMへ載せない)。結果の「演奏データ」(`src/sound/Music_Data.hpp`)は小さなバイト列で、
  音符1つが4バイト。繰り返しは展開せず「ここから」「n回戻る」の命令のまま持つ。マクロは展開する。
- 演奏データの置き場は**2つ**(6KiB×2)。2コア目が片方を読んでいる間に、もう片方へ次の曲を書く。
  2コア目が前の曲を手放す前に3曲目を頼むと「片付けが済んでいません」で断る(実機では1ms程度で手放すので、普通は起きない)。
  置き場と読み取り係(約3.5KB)は**最初に曲を鳴らすときに確保**し、以降は持ち続ける(曲を使わなければRAMを使わない)。
- 鳴らすのは2コア目の `MusicPlayer`。1ティック = 22050×60 / (テンポ×48) サンプルを**整数の積み上げ**で数え、
  音源の計算を「次のティックの境目」で区切るので、音符はサンプル単位で正しい位置に鳴り、長く鳴らしてもずれない。
- 効果音(`pico.sound_play`/`pico.beep`)はそのチャンネルを借りる。借りている間シーケンサーはそのチャンネルの音源に触らず、
  効果音の音が消えたら(長さ切れ・減衰し切った・止められた)返す。`pico.sound_stop()` は効果音だけを止め、曲の音は止めない。

## Luaから使う口

```lua
local ok, err = pico.music_play("/music/demo.mml")   -- 読み込んで鳴らす。失敗は nil, "3行12列: 理由"
pico.music_play_text("A o4 l8 cdefgab>c")            -- 文字列から直接(アプリに曲を埋め込む用)
pico.music_stop()
pico.music_playing()     -- -> boolean(頼んだ直後から結果の扱い)
```

- アプリを閉じたら曲も止める(`pico.sound_*`と同じ)。音量は `sound.cfg` の全体の音量に従う(曲だけの音量は持たない)。
- `pico.music_play` もSDの権限(`sd_outside_app_dir`)に従う。権限の無いアプリは自分のフォルダの中の曲だけ鳴らせる。

## ミュージックアプリ

ランチャの「ミュージック」(`MusicScene`)。SDの **`/music/` 直下の `*.mml`** を名前順に並べ、2回タップで鳴らす。
読めない曲は、行・列つきの理由を画面の下に赤で出す。アプリを閉じると曲も止まる。

## 決めたこと(2026-09-25)

1. `>` = オクターブを上げる(ppmck等と同じ)
2. `q` の既定は 7(少し切る)
3. ドラムはマクロで書く(専用の書き方は、使ってみて不便なら足す)
4. テンポ `t` は全チャンネル共通
5. 曲の置き場所は `/music/*.mml`。ミュージックアプリで鳴らす
6. ピッチの変化(ビブラート・ポルタメント等)は後回し(音源側にまだ機能が無い)
