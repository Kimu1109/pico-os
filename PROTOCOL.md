# pico-os ドキュメントサーバ プロトコル v1

pico-os の Markdown ブラウザが文書を取得・検索するための、サーバとの取り決め。

このプロトコルの目的は **「普通の静的ファイルサーバでも最低限成立し、対応する気があれば検索まで提供できる」** という段階的な対応を可能にすることにある。
文書の配信は素の HTTP GET でしかないので、`.md` ファイルを置いただけのサーバでも pico-os から読める。検索やマニフェストは任意で、対応していないサーバでは pico-os 側が該当機能を無効化する。

> **現状**: この仕様に対応する **pico-os 側の実装はまだ無い**(HTTPクライアント・キャッシュ層ともに未着手)。
> 本書は実装に先立って全体像を固めるための設計文書であり、実装の過程で版を上げずに修正されうる。
> v1 を確定と見なしてよいのは、リポジトリに参照実装サーバが入った時点とする。

## この文書の読み方

- **必須** … これを満たさないサーバは pico-os から正しく扱えない
- **推奨** … 満たさなくても動くが、満たすと体験が良くなる
- **任意** … 実装しなくてよい

## クライアント側の制約(仕様の根拠)

この仕様が素っ気ない形をしているのは、クライアントが以下の制約を持つためである。サーバを実装する際の前提として把握しておくとよい。

| 項目 | 値 | 影響 |
|---|---|---|
| MCU | RP2350 (Raspberry Pi Pico 2 W) | JSONパーサを積む余裕がない → 応答は行指向TSV |
| 画面 | 240 x 320 px | 検索結果は1件1行。長いタイトルは切れる |
| 文書サイズ上限 | 8,192 バイト | 超過分は切り捨てて表示(警告ログは出る) |
| 1ブロックの表示上限 | 512 バイト | 1段落が長すぎると途中で切れる |
| ブロック数上限 | 128 | 見出し・段落・リスト項目などの総数 |
| パス長上限 | 255 バイト | URL のパス部分がこれを超えると取得しない |
| 時計 | NTP同期前は不正 | 日付ベースの再検証より `ETag` が望ましい |
| 実行モデル | 単一スレッドのポーリングループ | 応答しないサーバは10秒で打ち切る |

文字コードは **UTF-8 固定**。クライアントのフォントは BMP 範囲のみを持つため、範囲外の文字は豆腐になる。

## トランスポート

- **必須**: HTTP/1.1 平文 (`http://`)。HTTPS は将来の版で扱う(下記「将来の拡張」参照)
- **必須**: クライアントは `Connection: close` を送る。サーバは応答後に接続を閉じてよい
- **必須**: サーバは **chunked transfer-encoding を使ってはならない**。クライアントは解釈しない
  - 本文の長さは `Content-Length` で示すか、長さを示さずに接続を閉じて終端とする
- **必須**: サーバは **クライアントが要求しない限り本文を圧縮してはならない**。クライアントは `Accept-Encoding` を送らない
- **推奨**: `Content-Length` を常に送る。クライアントは進捗表示と上限判定に使える
- クライアントは **Cookie を送らず、保持もしない**。認証を前提にしたサーバは対象外
- クライアントは **リダイレクト(301 / 302 / 307 / 308)を最大3回まで追う**。それを超えるとエラー扱いになる

## 共通のデータ形式 — TSV

discovery・検索・マニフェストの応答はすべて **タブ区切りの行指向テキスト** とする。JSONを使わないのは、クライアントにパーサが無く、追加すると flash と RAM を圧迫するため。行指向であれば **1行読むごとに1件処理でき、応答全体をバッファに溜める必要がない**。

- **必須**: `Content-Type: text/plain; charset=utf-8`
- **必須**: 各フィールドに **タブ・CR・LF を含めてはならない**。引用・エスケープの仕組みは持たない(パーサを単純に保つため)。含まれうる場合はサーバ側で空白等へ置換すること
- **必須**: 行の区切りは `LF` または `CRLF`
- **必須**: BOM を付けてはならない
- **推奨**: `#` で始まる行はコメントとして無視される。クライアントは読み飛ばす
- **前方互換のルール**:
  - クライアントは **知らないキー・知らない列を無視する**
  - サーバは **末尾の列を省略してよい**。クライアントは欠けた列を空文字として扱う
  - この2つにより、将来列やキーを足しても古いクライアントは壊れない

## エンドポイント

### 1. サーバ情報 (discovery) — 任意

```
GET /.well-known/pico-os
```

応答は `キー<TAB>値` の行の並び。

```
version		1
name		きむのドキュメント
home		/docs/index.md
search		/v1/search
manifest	/v1/manifest
```

| キー | 必須 | 意味 |
|---|---|---|
| `version` | 必須 | サーバが実装するプロトコルの版(整数)。現在は `1` |
| `name` | 任意 | サーバの表示名。pico-os のヘッダ等に出す |
| `home` | 任意 | このサーバの既定の文書パス |
| `search` | 任意 | 検索エンドポイントのパス。**この行が無いサーバは検索非対応**として扱われ、pico-os は検索ボタンを無効化する |
| `manifest` | 任意 | マニフェストエンドポイントのパス。無ければ都度の条件付きGETで再検証する |

- `search` / `manifest` のパスを仕様で固定していないのは、**既存サイトへ後付けする際にパスの自由度が要る**ため
- **このエンドポイントが 404 を返してもよい**。その場合クライアントは「文書配信のみ対応するサーバ」として扱い、検索を無効化して動作を続ける
- 上位の `version` を宣言するサーバは、**本仕様の必須要素を壊してはならない**(後方互換を保つこと)

### 2. 文書の取得 — 必須

```
GET /docs/pico/intro.md
```

- **必須**: 応答本文は Markdown のソースそのもの。サーバ側で HTML 等へ変換しない
- **推奨**: `Content-Type: text/markdown; charset=utf-8`。`text/plain` でもよい
- **推奨**: 1文書を **8,192バイト以内** に収める。超えた分はクライアントが切り捨てる
- **推奨**: `ETag` を返す。返せない場合は `Last-Modified` でもよい(下記「キャッシュと再検証」参照)

pico-os が解釈する Markdown の範囲は以下のとおり。これ以外の記法は素通しされ、そのままの文字列として表示される。

見出し(`#` / `##` / `###`) / 段落 / 箇条書き・番号付きリスト(ネスト可) / チェックボックス(表示専用) / 引用(ネスト可) / コードブロック / 水平線 / テーブル(**最大4列**、超過分は切り捨て) / 画像 / リンク / インラインの強調・コード。先頭の YAML フロントマターは読み飛ばされる。

### 3. 検索 — 任意

```
GET /v1/search?q=<検索語>&limit=10&offset=0
```

| パラメータ | 必須 | 意味 |
|---|---|---|
| `q` | 必須 | 検索語。URLエンコードされた UTF-8 |
| `limit` | 任意 | 返す最大件数。省略時はサーバの既定値。クライアントは **20以下**しか要求しない |
| `offset` | 任意 | 何件目から返すか(0始まり)。省略時は0 |

応答は1行1件の TSV。

```
docs/pico/intro.md	Pico入門	a1b2c3d4	RP2350はCortex-M33を2基搭載しており…
docs/pico/gpio.md	GPIOの使い方	e5f6a7b8	pinMode()で入出力方向を設定する…
```

| 列 | 必須 | 意味 |
|---|---|---|
| 1: `path` | 必須 | 文書のパス。そのまま GET できること |
| 2: `title` | 必須 | 表示用のタイトル。**画面幅の都合で先頭20文字程度しか読めない**ので、先頭に特徴が来るようにするとよい |
| 3: `version` | 推奨 | その文書の現在の検証子(`ETag` と同じ値)。**クライアントは手元のキャッシュが古いかを往復ゼロで判定できる** |
| 4: `snippet` | 任意 | 一致箇所の周辺。現在のクライアントは表示しないが、将来使う可能性がある |

- **必須**: 関連度の高い順に並べる。ランキングはサーバの責務
- **必須**: 返す件数は `limit` 以下
- 件数の総数は返さない。クライアントは **`limit` 件ちょうど返ってきた場合に「続きがあるかもしれない」** と判断して次ページを要求する
- 一致が無い場合は **200 で空の本文**を返す(404 にしない)

### 4. マニフェスト — 任意

キャッシュの再検証を一括で行うためのもの。文書数が多いサーバで、1文書ごとの条件付きGETの往復を省ける。

```
GET /v1/manifest
```

```
docs/pico/gpio.md	e5f6a7b8
docs/pico/intro.md	a1b2c3d4
docs/setup/wifi.md	11223344
```

| 列 | 必須 | 意味 |
|---|---|---|
| 1: `path` | 必須 | 文書のパス |
| 2: `version` | 必須 | 検証子(`ETag` と同じ値) |

- **必須**: **`path` の昇順(バイト列としての辞書順)に並べること。** クライアントは手元のキャッシュ目録と**先頭から突き合わせながら比較する**(全体をRAMに載せない)ため、順序が崩れると比較できない
- **推奨**: `ETag` を返す。マニフェスト自体も条件付きGETで再取得を省ける

## 画像とその他のリソース

- 画像は **`.pimg` 形式のみ**対応する。PNG / JPEG / GIF は表示できない
  - `.pimg` は pico-os 独自の形式で、16色固定パレットへ量子化したランレングス圧縮。リポジトリの `script/generate_pimg.py` で変換する
  - **サーバ側で事前に変換して置くこと。** クライアントに変換能力は無い
- 文書中から参照されるリソースは、文書と同じ規則で GET され、同じ規則でキャッシュされる
- 相対パスは**その文書の位置を基準**に解決される(一般的なブラウザと同じ)

## キャッシュと再検証

pico-os は取得した文書を **SDカードへ保存し、次回以降はそこから読む**。サーバ実装者が知っておくべき挙動は以下のとおり。

- クライアントは応答の `ETag` (無ければ `Last-Modified`) を保存する
- 次に同じ文書を開くとき、`If-None-Match` (または `If-Modified-Since`) を付けて GET する
- **推奨**: 変更が無ければ **`304 Not Modified`** を返す。本文を送らずに済み、クライアントは手元のキャッシュをそのまま使う
- **`ETag` が `Last-Modified` より望ましい。** クライアントの時計は NTP 同期前には信用できないため、日付ベースの比較は当てにならない
- `ETag` の中身は問わない(内容のハッシュでも、更新のたびに変わる連番でもよい)。**同じ内容なら同じ値、内容が変われば別の値**でありさえすればよい
- 検索応答やマニフェストの `version` 列は、**この `ETag` と同じ値**であること。別の体系にすると突き合わせができない

キャッシュの破棄はユーザ操作(リロード/全消去)で行える。サーバ側からキャッシュを無効化する手段は v1 では提供しない。

## 制限値の一覧

サーバ実装者が守る、あるいは想定しておく値。

| 対象 | 値 | 超えた場合 |
|---|---|---|
| 文書のサイズ | 8,192 バイト | 切り捨てて表示 |
| 1段落・1見出しの長さ | 512 バイト | その要素の表示が途中で切れる |
| 文書内のブロック数 | 128 | 以降のブロックは表示されない |
| テーブルの列数 | 4 | 5列目以降は切り捨て |
| URL のパス長 | 255 バイト | 取得しない |
| 検索結果の件数 | 1回あたり20件以下 | クライアントが要求しない |
| 応答までの時間 | 10秒 | 打ち切ってエラー表示 |
| リダイレクトの連鎖 | 3回 | 打ち切ってエラー表示 |

## エラー処理

クライアントが解釈するステータスコード。

| コード | 挙動 |
|---|---|
| `200` | 正常 |
| `304` | キャッシュを使う |
| `301` / `302` / `307` / `308` | `Location` を最大3回まで追う |
| `404` | 「文書が見つかりません」と表示。discovery の場合は「検索非対応のサーバ」として続行 |
| `4xx` / `5xx` (上記以外) | エラーとして表示。キャッシュは書き換えない |

**取得に失敗したとき、既存のキャッシュを壊してはならない** — これはクライアント側の責務だが、サーバ側も **部分的な応答を返した後に接続を切る**ような実装を避けること(クライアントは `Content-Length` に満たない応答を失敗として扱い、キャッシュを更新しない)。

## 最小構成のサーバ

`.md` ファイルを置いた静的ファイルサーバがあれば、それだけで pico-os から読める。

```sh
# これだけで「文書配信のみ対応」のサーバとして成立する
python3 -m http.server 8080
```

- `http.server` は `Last-Modified` と `If-Modified-Since` を既定で扱うため、**条件付きGETまで自動的に動く**
- discovery (`/.well-known/pico-os`) は 404 になるが、クライアントは検索を無効化して動作を続ける
- 検索まで対応したい場合は、`/.well-known/pico-os` を置いて `search` の行を足し、そのパスで TSV を返せばよい

## バージョニングと将来の拡張

- 現在のプロトコル版は **1**
- クライアントは discovery の `version` が自分の知らない上位版であっても、**v1 として通信を試みる**。上位版のサーバは v1 の必須要素を壊さないこと
- 以下は v1 では扱わないが、将来の版で扱う可能性がある。**今のうちに衝突しそうな設計をしないこと**を推奨する:
  - **HTTPS** — 対応予定。クライアント側の RAM(TLSバッファ)と、証明書検証に必要な時刻同期が課題。URL の scheme で区別するため、サーバ側の変更は不要の見込み
  - **keep-alive** — 現在は1リクエストごとに接続を閉じる
  - **全文検索以外の絞り込み** — タグ、更新日時の範囲など
  - **更新通知** — サーバ側からのキャッシュ無効化

## 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1 | 2026-09-14 | 初版 |
